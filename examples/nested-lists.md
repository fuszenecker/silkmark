# Nested lists and block containers

- Outer item
  - Inner bullet
    3. Deeper ordered item

- Item with a fenced code block:

  ```rust
  fn main() {
      println!("nested code");
  }
  ```

> A quote can contain lists:
> - quoted bullet
> 3. quoted ordered item
> - [x] quoted task

- A lazy continuation starts here
and continues on this physical line.

This paragraph starts after a blank block boundary.
